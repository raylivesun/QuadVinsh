import '../comments/grammars.dart';

var yi = Die("The following" as ArgumentError);

/// A parser for a single comment.
/// long comments are not allowed
/// the parser will return a single comment
/// parser will return a single comment
/// multiple comments are allowed
/// return a single comment
/// linked comments are not allowed

class SafeCommentParser {
  late String SafeComment = Safes(ArgumentError) as String;
}

class Safes {
  Safes(Type Error);
}

class SafeComment {
  late final ArgumentError error;
}


