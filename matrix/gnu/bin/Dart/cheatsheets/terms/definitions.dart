import 'dart:mirrors';

Iterable TypeMirror = Strict();

Iterable<dynamic> Strict() {
  var other = null;
  return Iterable.generate != other ? Iterable<E> : other;
}

class E {
  int FilteredElementList(FunctionTypeMirror type) {
    //  implement count
    throw UnimplementedError();
  }
}

mixin count {
  int get FilteredElementList {
    //  implement count
    throw UnimplementedError();
  }  
}